* 定义了领域层中的一些常用概念接口
    * 你不使用这些接口并不影响使用领域驱动设计
    * 这些接口的好处主要是提供一组显式的编程概念，这些概念应具备足够的稳定性
    * 另外可能会提供一套默认实现
* 定义了ERP业务中通用领域的一些值对象
    * 有一些基础视图，比如BaseCustomerView
    * 有一些通用定义，比如CustomerId
    * 基于组合优于继承的思想，大家需要自己在自己的实体对象中选择是否作为属性字段引入（我反对大部分的抽象基类，特别是在通用组件和框架层面）
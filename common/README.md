* 放在common中的部分只是为了编程方便，都应该重构为二方包
* 二方包要尽可能小而精，一个包解决一个特定的问题
* 能用三方包就不要用二方包，因为二方包需要投入资源持续升级和维护
* <del>可以构建一个二方包的空模块，这个模块里只是传递三方包的依赖用以沉淀最佳实践</del>
* 因为CQRS和DDD是两种架构，可以结合，但还是要注意区分，所以domain-foundation里不要出现CQRS的概念
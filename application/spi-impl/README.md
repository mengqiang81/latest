* 这个模块可以让定制化开发用GraaleJS脚本实现Application层定义的SPI接口
* 同时，通过表现层跟系统交互，但是这样的依赖就乱了(本质上是依赖SDK，不是直接依赖表现层)
* 要注意脚本的动静态的区别，对于大部分二开场景，我们希望脚本是静态编译的，可以发版。真正的动态脚本其实是给业务用的。
* 静态脚本放到resource下静态编译，运行时不可修改。动态脚本放到文件路径下，运行时动态加载。